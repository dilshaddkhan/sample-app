apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: production-app
  template:
    metadata:
      labels:
        app: production-app
    spec:
      containers:
      - name: production-container
        image: us-east1-docker.pkg.dev/qwiklabs-gcp-03-76dea61d28df/my-repository/hello-cloudbuild-dev:v1.0
        ports:
        - containerPort: 8080
  # Rollback trigger v1.0
